---
dist: trusty
language: python
sudo: required
cache:
  pip: true

install:
  - pip install molecule cookiecutter

script:
  - cookiecutter --no-input .
  - cd role && molecule lint

after_script:
  - git stash --all

notifications:
  email:
    on_failure: change
    on_success: never
