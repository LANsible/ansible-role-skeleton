---
dist: trusty
language: python
sudo: required
cache:
  pip: true

install:
  - pip install molecule cookiecutter

script:
  - cookiecutter --no-input --output-dir $TRAVIS_BUILD_DIR .
  - cd $TRAVIS_BUILD_DIR/role && molecule lint

notifications:
  email:
    on_failure: change
    on_success: never
